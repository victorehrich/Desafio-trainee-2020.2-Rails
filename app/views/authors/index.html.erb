<p id="notice"><%= notice %></p>

<h1>Authors</h1>

<table>
  <thead>
    <tr>
      <th style="padding:5px">Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @authors.each do |author| %>
      <tr>
        <td style="padding:5px"><%= author.name %></td>
        <td style="padding:5px"><%= link_to 'Show', author %></td>
        <td style="padding:5px"><%= link_to 'Edit', edit_author_path(author) %></td>
        <td style="padding:5px"><%= link_to 'Destroy', author, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Author', new_author_path,:class => "btn btn-primary" %>
